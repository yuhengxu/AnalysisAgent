# 14 · 待完善功能清单（TODO Backlog）

> 本清单供后续 AI / 开发者**逐项实现**。每条任务格式统一：
> **目标 · 涉及文件 · 实现要点 · 验收标准 · 难度（★1-5）**。
> 实现一条即可，互相独立。改动后务必：后端 `python -c "from app.main import app"`，前端 `npm run build`。

---

## A. 数据库（DB）

### TODO-DB-1 · 引入 Alembic 迁移 ★★★
- 目标：表结构变更可版本化迁移（当前 `create_all` 不会 ALTER 已存在表）。
- 涉及：新增 `backend/alembic/`、`alembic.ini`；`requirements.txt` 加 `alembic`。
- 要点：`alembic init`；以现有 `models` 生成基线迁移。
- 验收：改一个字段后 `alembic upgrade head` 能正确变更库。

### TODO-DB-2 · 声明外键约束 ★★
- 目标：`price_series.dataset_id`→`datasets.id` 等改为真实 ForeignKey。
- 涉及：各 `models/*.py`。
- 验收：删除 dataset 时相关数据按策略级联/限制。

---

## B. 数据导入（IMPORT）

### TODO-IMPORT-1 · 修复因素表 importance 解析 ★★★（重要）
- 目标：正确读取「下划线选中」的重要性(1-5)与影响(促涨/持平/促跌)。
- 涉及：`services/data_import.py::_import_factor_xlsx`。
- 要点：用 `openpyxl.load_workbook(path)`（**非 read_only，保留格式**），读 `cell.font.underline=='single'` 判断被选项（参考 `services/prediction.py` 导出时的下划线逻辑反向解析）。
- 验收：导入 `yuebao/prediction/2026年5月油价预测分析表.xlsx` 后，1.1 importance=3、impact=持平（与样例一致）。

### TODO-IMPORT-2 · 完善质量检查 ★★
- 目标：balance/factor 类别也做校验（空值率、必填列、日期连续性）。
- 涉及：`data_import.py::quality_check`。
- 验收：返回结构化 `stats` 并在前端 DataCenter 展示。

### TODO-IMPORT-3 · 接入外部数据 API（Wind/EIA 等） ★★★★
- 目标：定时从权威 API 拉数入库（`source_type="api"`）。
- 涉及：新增 `services/data_fetch.py` + apscheduler 定时任务（依赖已在 requirements）。
- 验收：可配置任务，按计划写入 price_series 并记录来源。

---

## C. 数据分析（ANALYTICS）

### TODO-ANALYTICS-1 · 区分环比/同比 ★★ — **已完成**
- 目标：`calc_monthly_stats` 同时返回 `mom_pct`（环比，较上月）与 `yoy_pct`（同比，较去年同月）。
- 涉及：`services/analytics.py`；表1-1 由 `build_table_price_change_rows()` 使用两项指标。
- 验收：返回含 `mom_pct` 与 `yoy_pct` 且口径正确；详见 [04-数据处理与分析模块](./04-数据处理与分析模块.md)。

### TODO-ANALYTICS-2 · SQL 下推聚合 ★★★
- 目标：大数据量下用 SQL `GROUP BY` 替代 pandas 内存聚合。
- 涉及：`analytics.py` 各聚合方法。
- 验收：万级数据查询明显提速。

---

## D. 统计预测（FORECAST）

### TODO-FORECAST-1 · 升级预测模型 ★★★★
- 目标：用 ARIMA/Prophet/梯度提升替代 MA3+趋势（statsmodels/sklearn 已装）。
- 涉及：`services/forecast.py::run_forecast`，新增 `model_name`。
- 验收：`/forecast/backtest` 的 MAPE 较现状下降。

---

## E. 预测分析表（PREDICT）

### TODO-PREDICT-1 · 证据从 DB 升级为实时联网取数 ★★★★★
- 目标：`gather_evidence` 真正从可信数据源（EIA/IEA/OPEC/CFTC…）抓取最新指标，作为 LLM 证据。
- 涉及：`skills/prediction_skill.py::gather_evidence` + 新增抓取层；遵守各源使用条款。
- 验收：judgment 中引用的数据可追溯到当期真实数值。

### TODO-PREDICT-2 · 历史月份对比与趋势图 ★★
- 目标：在 Prediction 页展示同一因素近几个月 importance/impact 变化。
- 涉及：后端聚合 + 前端 `pages/Prediction.tsx`。

---

## F. 月报（REPORT）

### TODO-REPORT-1 · Word 内嵌图表/目录/页眉页脚 ★★★ — **部分完成**
- 目标：导出 docx 含价格走势图（matplotlib 出图嵌入）、自动目录、页眉。
- 已完成：matplotlib PNG 嵌入 + 中文字体主题 + 前端 ReportChartImage 预览 + **6 张**样例图表锚点（图1-1～1-6）+ **6 张系统表 snapshot 预置**。
- 剩余：自动目录、页眉页脚；图1-2/1-3 待 CFTC/ICE 持仓数据源。

### TODO-REPORT-2 · 支持周报 ★★★
- 目标：新增 `report_type=weekly` 模板与 skill 分支。
- 涉及：`templates/`、`report_skill.py`、`report.py`。

---

## G. Agent / 大模型（AGENT）

### TODO-AGENT-1 · 流式输出（SSE） ★★★
- 目标：对话与生成支持流式返回，改善长任务体验。
- 涉及：`core/llm.py`(stream=True) + `api/agent.py`(StreamingResponse) + 前端 EventSource。

### TODO-AGENT-2 · 新增国产大模型 provider ★★
- 目标：通义/文心/Kimi 等。
- 涉及：`core/llm.py::_resolve` + `config.py`。

---

## H. 可视化（VIZ）

### TODO-VIZ-1 · 图表导出为图片并入库 ★★★ — **部分完成**
- 目标：把分析图表渲染为 PNG 存 `data/charts/`，供报告引用。
- 已完成：月报导出时生成 PNG + 前端 ReportCenter 预览。
- 剩余：Analysis 页图表一键导出 PNG。

---

## I. 前端（FE）

### TODO-FE-1 · 章节富文本编辑 ★★
- 目标：ReportCenter 用已安装的 tiptap 替代 textarea，支持加粗/列表。

### TODO-FE-2 · 路由级懒加载 ★★
- 目标：`React.lazy` 拆分各页面，减小首包（当前 >500KB 警告）。

---

## J. 工程化（OPS）

### TODO-OPS-1 · 自动化测试 ★★★
- 目标：pytest 覆盖 services/skills（mock LLM）；前端 vitest。
- 涉及：新增 `backend/tests/`。

### TODO-OPS-2 · 鉴权与多用户 ★★★★ — **已完成**
- 目标：登录、JWT、按用户隔离数据、页面权限、用户管理。
- 实现：
  - `users` 表 + JWT + admin/user 角色
  - 用户管理 API（`/users/*`）+ 前端 `UserManagement.tsx`
  - 页面权限 `require_page` + 前端 `PageRoute` / `canAccessPage`
  - reports/predictions **严格**按 `user_id` 隔离（admin 亦不例外）
  - 异步任务绑定 `user_id`，轮询校验归属
- 默认 admin：env `ADMIN_USERNAME` / `ADMIN_PASSWORD`（按用户名查找或创建，见 `user_seed.py`）。
- 文档：[17-用户管理与权限](./17-用户管理与权限.md)

### TODO-OPS-3 · 结构化日志 + trace_id ★★★
- 目标：见 13 节扩展点。
