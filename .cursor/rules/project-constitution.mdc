---
description: 能源 AI 数据分析平台 项目核心约束 - 所有开发工作不可偏离的底线
alwaysApply: true
---

# 项目宪法约束

来自 [README.MD](../../README.MD)，所有对话中必须遵守。

## 系统边界

- 平台面向**能源行业（国际原油）**的数据分析、预测与月报生成
- 两大核心交付物：**油价预测分析表**（Excel）与 **国际油价月报**（Word）
- Agent 为统一入口，驱动数据分析、预测表、月报等 skill，不是外挂补丁

## 部署与环境

- 支持 Windows 和 Linux 本地开发；Docker Compose 一键部署
- 存储：SQLite + 本地文件（`data/`、`db/`、`logs/`）
- 生产异步任务（月报/预测生成）为**单进程内存态**，`uvicorn --workers` 必须为 **1**

## 安全与权限（硬约束）

- **JWT 鉴权**：除 `/auth/login` 外，业务 API 须登录
- **页面权限**：后端 `require_page(key)` + 前端 `PageRoute` / `canAccessPage`，禁止只做前端隐藏
- **数据隔离**：月报、预测分析表严格按 `user_id == 当前用户.id` 隔离（admin 亦不例外）
- **异步任务隔离**：`task.user_id` 须与当前用户一致，越权返回 404
- **密钥不入库**：`.env` 管理 API Key；密码 bcrypt 哈希存储

## 大模型与 Skill（硬约束）

- 统一客户端 `core/llm.py`；skill 层负责「证据采集 + LLM + 结构化产出 + 降级」
- **LLM 不可用时必须降级**：规则兜底，接口不得因模型失败而 500
- **月报 6 张系统表**：只读 snapshot（`report_table_snapshots`），生成/导出时不得让 LLM 填 rows
- **结构化 JSON**：预测表/月报存 `content_json`，前端编辑与后端导出共用同一结构

## 分层架构（硬约束）

- `api/` 只做编排，不写业务逻辑
- `services/` 做 DB 读写、文件产出、编排 skill
- `skills/` 不直接处理 HTTP
- 新增接口：`api/` → `services/` → `client.ts` → 页面

## 工程约束

- **文档驱动**：`docs/` 为权威设计文档；`plans/` 为实施方案；禁止跳过文档直接大改架构
- **代码溯源**：输出代码时声明依据文档（如「依据 docs/07 §5.1」）
- **变更联动**：表/接口/权限/页面 key 变更时，同步更新 `docs/` 对应章节
- **最小 diff**：只改任务相关文件，不顺手重构无关代码
